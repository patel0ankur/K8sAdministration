apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: default
spec:
  clusterIP: 10.105.64.201
  ports:
  - nodePort: 30080
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    name: webapp
  type: NodePort
