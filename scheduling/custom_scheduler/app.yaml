# create a custom scheduler using 'custom_scheduler.yaml' file. Use new schedulerName: my-scheduler

apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  -  image: nginx
     name: nginxmaster
  schedulerName: my-scheduler